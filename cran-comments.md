## Test environments

* local macOS, R release

## R CMD check results

0 errors | 0 warnings | 1 note

* The note is the standard "New submission" note: this is the first CRAN
  submission of `aisdk.skills`.

## Downstream dependencies

There are currently no downstream dependencies for this package.

## Comments

This is the first CRAN submission of `aisdk.skills`. It is a companion package for
`aisdk` and uses that package's exported extension API; all of its other
heavier dependencies are declared under Suggests and used conditionally.
